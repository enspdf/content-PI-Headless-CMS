type App {
  id: UUID!
  appName: String!
  icon: String!
  description: String!
  createdAt: DateTime!
  updatedAt: DateTime!
}

type Query {
  getApps: [App!]
}

type Mutation {
  createApp(input: CreateInputApp): App!
}

input CreateInputApp {
  appName: String!
  icon: String!
  description: String!
}
